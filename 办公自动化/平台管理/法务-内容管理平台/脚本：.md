### 启动
```
exec ./deploy/docker-up.sh test

```
### 更新规则：

**本机：**

```
cd /Users/wenkai/it/cursor/LexiReview
uv run python -m lexireview.cli.import_rules
```

**Docker 容器内：**

```
docker compose exec api python -m lexireview.cli.import_rules
```
