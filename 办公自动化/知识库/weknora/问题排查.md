1.定位文件
```
docker exec WeKnora-postgres psql -U weknora -d weknora -c "
select id, title, file_type, parse_status, error_message, created_at, updated_at
from knowledges
where title ilike '%分贝通报销流程%'
order by created_at desc
limit 5;
"
```
2.